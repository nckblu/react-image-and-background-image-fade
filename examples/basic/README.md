# Basic Example

This folder shows the intended V2 API shape inside an app.

The package is designed to work with Vite, Remix, Next client components, and other React runtimes:

```tsx
import { Image, BackgroundImage, Picture } from 'react-image-and-background-image-fade'
import 'react-image-and-background-image-fade/styles.css'
```
